apiVersion: v1
kind: Service
metadata:
  name: inscription
  namespace: slangenglish
spec:
  selector:
    app: inscription
  ports:
    - protocol: TCP
      port: 8030
      targetPort: 8030
  type: ClusterIP
