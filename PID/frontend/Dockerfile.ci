# CI/Jenkins: build happens in pipeline (npm ci + npm run build).
FROM nginx:1.27-alpine

COPY nginx/default.conf /etc/nginx/conf.d/default.conf
COPY dist/my-project/browser /usr/share/nginx/html

EXPOSE 80

