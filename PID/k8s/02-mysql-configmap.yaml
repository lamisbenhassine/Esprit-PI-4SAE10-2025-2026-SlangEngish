apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-initdb
  namespace: slangenglish
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS userdbb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
    CREATE DATABASE IF NOT EXISTS reclamationdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
