# Copy to mysql-secret.yaml, set MYSQL_ROOT_PASSWORD, apply:
#   kubectl apply -f mysql-secret.yaml
# Do not commit mysql-secret.yaml with a real password.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: pid
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "CHANGE_ME"
