apiVersion: apps/v1
kind: Deployment
metadata:
  name: evaluation-service
  namespace: pid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: evaluation-service
  template:
    metadata:
      labels:
        app: evaluation-service
    spec:
      containers:
        - name: evaluation-service
          image: lfray/pid-evaluation-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8020
          env:
            - name: DB_HOST
              value: mysql
            - name: DB_PORT
              value: "3306"
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/evaluatiob?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: EUREKA_CLIENT_ENABLED
              value: "false"
---
apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: pid
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - port: 8020
      targetPort: 8020
